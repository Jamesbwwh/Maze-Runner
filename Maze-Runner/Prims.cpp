#include "stdafx.h"
#include "Prims.h"
//#include <set>

void Prims::generate(Maze maze) {
	srand(time(NULL));
	//set<int, int> theSet;
	int counter = 0;
	//theSet.insert(rand() % maze.getRow(), rand() % maze.getCol());
}
